services:
  - type: web
    name: sistema-de-pedidos
    env: python
    plan: free
    buildCommand: pip install -r SistemaDePedidos/requirements.txt && python SistemaDePedidos/manage.py collectstatic --noinput
    startCommand: gunicorn SistemaDePedidos.SistemaDePedidos.wsgi:application --chdir SistemaDePedidos

    envVars:
      - key: DEBUG
        value: False
      - key: SECRET_KEY
        generateValue: true
      - key: DATABASE_URL
        fromDatabase:
          name: sistema-de-pedidos-db
          property: connectionString
      - key: DJANGO_SETTINGS_MODULE
        value: SistemaDePedidos.settings
      - key: PYTHON_VERSION
        value: 3.13.4

databases:
  - name: sistema-de-pedidos-db
    plan: free
